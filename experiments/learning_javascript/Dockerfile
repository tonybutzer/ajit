from node

workdir /home/node

run git clone https://github.com/n-riesco/ijavascript.git

#run apt-get install jupyter

run apt-get -y update && apt-get install -y python3-pip python3-dev && pip3 install --upgrade pip

#run npm config set prefix $HOME

#run npm install -g ijavscript && ijsinstall


